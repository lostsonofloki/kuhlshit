# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Production
/dist
/build

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
Desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scraping
*.png
*.jpg
*.webp
!public/resources/**/*.png
!public/resources/**/*.jpg
!public/resources/**/*.webp
stifftones-debug.png

# Misc (root only — do not use `archive/` or it ignores `docs/archive/`)
/archive/
# Root HTML exports (not the Vite app shell)
/*.html
!/index.html
# Playwright audit snapshots next to repo root (legacy output dir)
/audit/

# Local AI / editor tooling
.qwen/

# Vercel CLI — commit project.json for shared link; ignore local cache
.vercel/*
!.vercel/project.json

# Image-optimizer incremental state
scripts/.kuhl-opt.json

# Local scratch
tmp/

# Lighthouse / PSI CLI dumps (not committed)
/lighthouse*.json
/psi*.json

# Ad-hoc downloads / archives at repo root (not site assets)
/Porchfest*.zip

# Scratch media outside public/ (ship via public/resources/)
/resources/

# Generated page-capture output (see docs/archive/audit-page-capture.mjs)
/docs/archive/audit-output/